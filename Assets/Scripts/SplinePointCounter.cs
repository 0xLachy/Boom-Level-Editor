using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SplinePointCounter : MonoBehaviour
{
    //TODO: Fix the other error that Mattatz has with NULL reference exception
    //The whole purpose of this is to avoid the spline point count bug
    public int pointCount = 4;
}
